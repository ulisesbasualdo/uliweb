import { VerticalStepperDirective } from './vertical-stepper.directive';

describe('VerticalStepperDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticalStepperDirective();
    expect(directive).toBeTruthy();
  });
});
